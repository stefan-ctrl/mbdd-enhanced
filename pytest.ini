[pytest]
# Quiet output, limited summary (no PASSED), top 10 durations,
# and JUnit XML to reports/junit.xml. Coverage is enabled in CI via CLI args.
addopts = -q -r fEsxX --durations=10 --junitxml=reports/junit.xml
junit_family = xunit2
